<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Cambiar Contraseña - Artesanos.com</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="icon" href="/favicon.png" type="image/png">
</head>
<body class="min-h-screen" style="background-image: url('/fondo.png'); background-size: cover;">
    <%- include('partials/navbar') %>

    <div class="fixed inset-0 backdrop-blur-md bg-black/10 z-0"></div>

    <div class="flex justify-center items-center my-20">
        <div class="bg-orange-200/50 backdrop-blur-sm rounded-3xl p-10 shadow-2xl border border-orange-300/60 w-full max-w-lg relative z-10">
            <h2 class="text-2xl font-bold mb-4 text-white">Cambiar Contraseña</h2>
            
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="bg-red-500/80 text-white p-3 rounded-lg mb-4 text-center">
                    <%= error %>
                </div>
            <% } %>

            <form action="/usuario/cambiar-clave" method="POST" class="flex flex-col gap-4">
                <div>
                    <label class="block text-white font-semibold mb-1">Contraseña Actual</label>
                    <input type="password" name="clave_actual" required class="border rounded-lg px-3 py-2 w-full">
                </div>
                <div>
                    <label class="block text-white font-semibold mb-1">Nueva Contraseña</label>
                    <input type="password" name="clave_nueva" required class="border rounded-lg px-3 py-2 w-full">
                </div>
                <div>
                    <label class="block text-white font-semibold mb-1">Confirmar Nueva Contraseña</label>
                    <input type="password" name="clave_confirmar" required class="border rounded-lg px-3 py-2 w-full">
                </div>
                
                <div class="flex flex-col gap-2 mt-4">
                    <button type="submit" class="text-white bg-gradient-to-r from-cyan-400 via-cyan-500 to-cyan-600 font-medium rounded-lg text-md px-5 py-2 hover:scale-105 hover:shadow-md transition-all">
                        Actualizar Contraseña
                    </button>
                    <a href="/usuario/editar" class="text-center text-white/80 hover:text-white transition-colors text-sm">
                        Volver a edición de perfil
                    </a>
                </div>
            </form>
        </div>
    </div>

    <%- include('partials/icons') %>
</body>
</html>
