<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>Cambiar Contraseña - Artesanos.com</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="icon" href="/favicon.png" type="image/png">
</head>

<body class="min-h-screen" style="background-image: url('/fondo.png'); background-size: cover;">
    <%- include('partials/navbar') %>

        <div class="fixed inset-0 backdrop-blur-md bg-black/10 z-0"></div>

        <div class="flex justify-center items-center min-h-screen py-20 px-4 md:px-0">
            <div
                class="bg-orange-200/50 backdrop-blur-md rounded-3xl p-8 md:p-10 shadow-2xl border border-orange-300/60 w-full max-w-lg relative z-10">
                <h2 class="text-3xl font-bold mb-6 text-white text-center">Cambiar Contraseña</h2>

                <% if (typeof error !=='undefined' && error) { %>
                    <div
                        class="bg-red-500/80 text-white p-3 rounded-xl mb-6 text-center text-sm font-bold border border-white/20">
                        <%= error %>
                    </div>
                    <% } %>

                        <form action="/usuario/cambiar-clave" method="POST" class="flex flex-col gap-5">
                            <div>
                                <label
                                    class="block text-white text-xs font-bold mb-1 uppercase tracking-wider">Contraseña
                                    Actual</label>
                                <input type="password" name="clave_actual" required
                                    class="bg-white/10 border border-white/20 rounded-xl px-4 py-3 w-full text-white focus:outline-none focus:border-cyan-400">
                            </div>
                            <div>
                                <label class="block text-white text-xs font-bold mb-1 uppercase tracking-wider">Nueva
                                    Contraseña</label>
                                <input type="password" name="clave_nueva" required
                                    class="bg-white/10 border border-white/20 rounded-xl px-4 py-3 w-full text-white focus:outline-none focus:border-cyan-400">
                            </div>
                            <div>
                                <label
                                    class="block text-white text-xs font-bold mb-1 uppercase tracking-wider">Confirmar
                                    Nueva Contraseña</label>
                                <input type="password" name="clave_confirmar" required
                                    class="bg-white/10 border border-white/20 rounded-xl px-4 py-3 w-full text-white focus:outline-none focus:border-cyan-400">
                            </div>

                            <div class="flex flex-col gap-4 mt-4">
                                <button type="submit"
                                    class="w-full bg-gradient-to-r from-cyan-400 to-cyan-600 font-bold text-white rounded-xl py-4 hover:scale-[1.02] transition-all shadow-lg shadow-cyan-500/20 uppercase tracking-widest">
                                    Actualizar Contraseña
                                </button>
                                <a href="/usuario/editar"
                                    class="text-center text-cyan-200 hover:text-white transition-colors text-sm font-semibold">
                                    &larr; Volver a edición de perfil
                                </a>
                            </div>
                        </form>
            </div>
        </div>

        <%- include('partials/icons') %>
</body>

</html>